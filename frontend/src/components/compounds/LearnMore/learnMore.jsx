import {Section, SectionImage, SectionWrapper, SectionText} from '../../elements'

export const LearnMore =({details})=>{
    return (
        <Section>
            <SectionImage/>
            <SectionText/>
            <SectionWrapper/>
            <SectionWrapper/>
        </Section>
    )
}